<% action ||= "show" %>
<% object ||= {} %>
<% modal_id ||= "#{channel}-modal" %>

<div id="turbo-consumer">
  <div
    data-controller="modal"
    data-modal-modal-id-value="<%= modal_id %>"
    data-modal-action-value="<%= action %>"
    data-modal-object-value="<%= object.to_json %>"
  ></div>
</div>